import { HomeTemplate } from "@/src/features/dashboard";

export default function DashboardHomePage() {
    return (
        <section className="w-full h-full">
            <HomeTemplate />
        </section>
    )
}